diff --git a/sidecar/vendor/core/parser.ts b/sidecar/vendor/core/parser.ts
index fd5cbab..f2ffc9a 100644
--- a/sidecar/vendor/core/parser.ts
+++ b/sidecar/vendor/core/parser.ts
@@ -99,7 +99,12 @@ function pct(phase: number, intraPhase: number): number {
 }
 
 export function findLogsDirs(): string[] {
-  return [...findVsCodeDirs(), ...findXcodeDirs()];
+  // Fork divergence (decision D8): the JetBrains port ships v1 with CLI-harness
+  // collectors only (Claude Code, Codex, OpenCode, Copilot CLI), which are
+  // gathered by `collectExternalHarnesses` independently of this list. VS Code
+  // workspaceStorage and Xcode discovery are dropped from the path because the
+  // VS Code extension owns its own cache and Xcode is out of scope for v1.
+  return [];
 }
 
 function partitionDirs(logsDirs: string[]): { vsCodeDirs: string[]; xcodeDirs: string[] } {
diff --git a/sidecar/vendor/core/parser-main.test.ts b/sidecar/vendor/core/parser-main.test.ts
index f0320f3..511537a 100644
--- a/sidecar/vendor/core/parser-main.test.ts
+++ b/sidecar/vendor/core/parser-main.test.ts
@@ -138,11 +138,14 @@ describe('LOAD_PHASES', () => {
 });
 
 describe('findLogsDirs', () => {
-  it('combines VS Code and Xcode directories', () => {
+  it('drops VS Code and Xcode discovery (fork divergence D8: CLI harnesses only)', () => {
+    // The JetBrains port collects CLI harnesses (Claude Code, Codex, OpenCode,
+    // Copilot CLI) via collectExternalHarnesses, independent of this list, and
+    // ships v1 without VS Code workspaceStorage / Xcode discovery.
     vi.mocked(findVsCodeDirs).mockReturnValue(['/logs/vscode-a', '/logs/vscode-b']);
     vi.mocked(findXcodeDirs).mockReturnValue([XCODE_DIR]);
 
-    expect(findLogsDirs()).toEqual(['/logs/vscode-a', '/logs/vscode-b', XCODE_DIR]);
+    expect(findLogsDirs()).toEqual([]);
   });
 });
 
